import { DeletespacePipe } from './deletespace.pipe';

describe('DeletespacePipe', () => {
  it('create an instance', () => {
    const pipe = new DeletespacePipe();
    expect(pipe).toBeTruthy();
  });
});
